name: pipelene
on:
  push:
    branches:
    - feat-pipeline
jobs:
  SAST:
    runs-on: ubuntu-latest #node:19-alpine3.15
    steps:
    - name: Set up JDK 17
      uses: action/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Checkout code
      uses: actions/checkout@v2

    - name: build
      run: |
        npm install
